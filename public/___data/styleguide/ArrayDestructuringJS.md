```js
// Extract data from arrays or objects
// Assign array or object properties to variables using array/object literal syntax

// Destructure multiple parameters into an object
function createMenu({ title, body, buttonText, cancellabe}) {
    ...
}

// no foo
createMenu({
    title: "foo",
    body: "",
    buttonText: "",
    cancellable: true
});
```

Swapping Elements

```js
let var1 = value1;
let var2 = value2;

[value1, value2] = [value2, value1];

let identifierSide = ValueSide
```