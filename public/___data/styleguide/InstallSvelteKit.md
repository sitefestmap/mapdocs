```shell
npm create svelte@latest my-app
cd my-app
npm install
git init && git add -A && git commit -m "Initial commit" # optional
# default port:5473 - change in vite.config
npm run dev -- --open
```