import { Backlinks, LinkList } from 'components';
export const title = 'Export';
export const backlinks = [
    { title: 'Controls', url: 'controls' },
]
export const links = [
    { title: 'Export Demo', url: 'https://watergis.github.io/mapbox-gl-export/#12/-1.09478/35.88265', icon: 'mapbox.png' }
]

# {title}

<Backlinks backlinks={backlinks}/>

There is an option to add a UI control for exporting a static Map to PNG, PDF, SVG, or JPEG.